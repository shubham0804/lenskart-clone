import React from "react";
import { StyleSheet, Text, View } from "react-native";

const GridView = ({ product }) => {
    return (
        <View>
            <Text>Grid view Product</Text>
        </View>
    );
};

export default GridView;

const styles = StyleSheet.create({});
